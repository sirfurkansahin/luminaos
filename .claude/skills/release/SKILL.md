---
name: release
description: Run when cutting a release (versioning, changelog, migration order, canary checklist). Skeleton only — full procedure fills in once F0-T3's CI pipeline has a real deploy stage (out of scope for F0-T3, planned for the end of Phase 1).
---

**Status: skeleton — to be filled in at the end of Phase 1.**

This skill will cover versioning, changelog generation, migration ordering, and the canary-rollout checklist for cutting a release, once deployment/CD exists (explicitly out of scope for F0-T3's CI work).

Until then, if asked to cut a release, treat this as out of current scope and flag it — there is no deploy pipeline yet.
