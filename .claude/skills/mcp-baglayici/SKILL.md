---
name: mcp-baglayici
description: Run when adding a new MCP connector (identity/auth, rate limiting, error contract). Skeleton only — full procedure fills in during Phase 2 (MCP client/server work, docs/PLAN.md vision item G).
---

**Status: skeleton — to be filled in during Phase 2.**

This skill will cover the procedure for adding a new MCP connector once `packages/integrations` (MCP client/server, vision item G) exists: identity/auth setup, rate-limit handling, and the connector's error contract.

Until then, if asked to add an MCP connector, treat this as out of current scope and flag it — the underlying package doesn't exist yet.
