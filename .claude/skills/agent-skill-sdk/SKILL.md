---
name: agent-skill-sdk
description: Run when adding a new skill package to the Agent Runtime (docs/PLAN.md vision item J). Skeleton only — full procedure fills in during Phase 3 once packages/agent-runtime exists.
---

**Status: skeleton — to be filled in during Phase 3.**

This skill will cover the standard for adding a new skill package to the Agent Runtime (`packages/agent-runtime`, vision item J — OS-level agent runtime + permission manifests) once that package exists.

Until then, if asked to add an agent-runtime skill package, treat this as out of current scope and flag it — the underlying package doesn't exist yet.
