# Install as /etc/luminaos/caddy.env with mode 0600. Never commit the real value.
# Use the same value for the Cloudflare Pages secret API_PROXY_SECRET.
API_PROXY_SECRET=replace-with-at-least-32-random-base64-characters
API_HOST=api.example.com
API_UPSTREAM=127.0.0.1:3000
