# F1-T10 — Görev Deneyimi (Durum, Öncelik, Kontrol Listesi, Yinelenen, Hatırlatıcı)

**Epik:** F1-E3 (Görev + Doküman + Takvim Çekirdeği) · **Durum:** Tamamlandı (Kabul Kriterleri 5/5 + madde 6 UI kapsamı PR6a-g ile kapandı)
**Bağımlılık:** F1-T1 (varlık çekirdeği), F1-T2 (custom fields), F1-T3 (ilişki sistemi), F1-T6 (sorgu katmanı)

## Amaç

`task` nesne tipini gerçek bir görev deneyimine dönüştürmek: durum/öncelik, kontrol listesi, yinelenen görevler, hatırlatıcı. Tasarım ilkesi: mümkün olduğunca F1-T1/T2/T3'te zaten kurulan genel mekanizmalar (Custom Fields, ilişkiler, event sourcing) yeniden kullanılır — görev için özel bir paralel veri modeli açılmaz.

## Kapsam

1. **Durum/Öncelik varsayılan alanları:** Yeni workspace oluşturulduğunda `task` tipi için iki `select` Custom Field otomatik provizyone edilir (F1-T2 mekanizmasıyla, idempotent seed — F0-T5 workspace oluşturma akışına eklenir): `status` (Yapılacak/Sürüyor/Bitti, seçenekler silinemez yalnızca yeni eklenebilir) ve `priority` (Düşük/Orta/Yüksek/Acil). Bu tasarım sayesinde F1-T7'deki Board görünümü ekstra kod olmadan `status`'a göre gruplanabilir.
2. **`select` alan seçeneklerine `isDone` bayrağı:** F1-T2'nin seçenek şemasına opsiyonel `isDone: boolean` eklenir (yalnız `status` alanında anlamlı; varsayılan seed'de "Bitti" seçeneğinde `true`). Bu bayrak, madde 4'teki yinelenen görev tetikleyicisinin veri kaynağıdır.
3. **Kontrol listesi (checklist):** Ayrı LuminaObject açılmaz — `packages/core-objects`'e gömülü `ChecklistItem[]` değer tipi eklenir: `{ id(ULID), text, done: boolean, order: number }`. Komutlar: `addChecklistItem/toggleChecklistItem/removeChecklistItem/reorderChecklistItem` → olaylar `ChecklistItemAdded/Toggled/Removed/Reordered`. Üst sınır: görev başına 200 öğe (aşımda `ValidationError`).
4. **Yinelenen görevler:** Task'a `recurrenceRule` alanı (`{ frequency: 'daily'|'weekly'|'monthly', interval: number, byWeekday?: number[], endDate?: string }`). `status` alanı `isDone=true` seçeneğine geçtiğinde (madde 2), bir sonraki tekrarın tek bir yeni `task` nesnesi olarak üretilmesi tetiklenir; yeni nesne F1-T3 ilişki sistemiyle önceki nesneye `recurrence-of` ilişkisiyle bağlanır. Üretim idempotent olmalı (aynı tamamlanma olayı için ikinci kez tetiklenmez — olay ID'si ile kilitlenir).
5. **Hatırlatıcı:** `remindAt` (datetime) + `remindAcknowledged` (boolean) alanları. OS-seviyesi push bildirimi bu görevin kapsamında DEĞİL (bkz. Kapsam Dışı); bunun yerine F1-T6 sorgu katmanı üzerinden `remindAt <= now() AND remindAcknowledged = false` sorgusu açık istemcide 60 sn'de bir çalışır, süresi geçen hatırlatıcılar uygulama içi bildirim (toast/badge) olarak gösterilir; kullanıcı görünce `ReminderAcknowledged` olayı üretilir.
6. **UI:** Görev detay panelinde durum/öncelik seçiciler, kontrol listesi widget'ı, yinelenme kural seçici, hatırlatıcı seçici — `packages/ui` (F0-T7) bileşenleriyle.

## Kapsam DIŞI

- OS-seviyesi/push bildirimleri (Tauri native notification, web push, mobil push) — otomasyon motoruyla (F2-E5) birlikte ele alınacak.
- Atama/mention bildirim sistemi.
- Çoklu kullanıcının aynı kontrol listesini eşzamanlı düzenlerken çakışma çözümü (CRDT) — F1-T11'deki doküman CRDT altyapısından ayrı, bu görevde son-yazan-kazanır yeterli.
- Takvimden otomatik hatırlatıcı türetme (F1-T12).

## Kabul Kriterleri

- [x] Yeni workspace oluşturulduğunda `task` tipi için `status`/`priority` alanları otomatik var olur (entegrasyon testli); seed ikinci kez çalıştırılırsa yinelenmez (idempotent, testli). (PR1, #34)
- [x] Kontrol listesi: ekleme/işaretleme/silme/yeniden sıralama olayları `replayObject` ile doğru sırayla katlandığı property-based testle kanıtlı. Komut katmanı + `replayObject` katlama PR2'de (#35) birim testleriyle kanıtlandı; property-based (fast-check) kapsam ayrı bir test-only PR'da (`checklist-replay-property.test.ts`) eklendi — `replay-property.test.ts`'in `fc.commands`/`fc.modelRun` deseniyle, rastgele geçerli add/toggle/remove/reorder dizileri için `replayObject`'in katladığı `checklist`'in bağımsız bir modelle her adımda birebir eşleştiği kanıtlandı; implementasyon değişmedi, ilk seferde yeşil.
- [x] `status` alanı `isDone=true` seçeneğine geçince tam olarak bir yeni yinelenen görev üretildiği, önceki göreve `recurrence-of` ilişkisiyle bağlandığı testli; aynı tamamlanma olayının iki kez işlenmesi ikinci nesneyi üretmediği (idempotency) testli. Kanıt iki dosyaya bölünmüş durumda: `TaskRecurrenceService.generateNextOccurrence`'ın kendisi (PR3, #38) idempotency dahil izole testli; `status`→`isDone` geçiş TESPİTİ + `ObjectsService.setFieldValues`'in onu doğru `causationEventId`/`sourceObjectId`/`nextOccurrence` ile çağırdığı (PR4, #40) gerçek Postgres+HTTP entegrasyon testiyle kanıtlı — `causationEventId` eşleşmesi security-reviewer tarafından da doğrulandı.
- [x] Hatırlatıcı: `remindAt` geçmiş ve `remindAcknowledged=false` olan görevler sorgu katmanından doğru döner; kullanıcı gördükten sonra `ReminderAcknowledged` olayı üretilip tekrar listelenmediği testli. (PR5, #43) `remindAt`/`remindAcknowledged` F1-T2 Custom Field olarak uygulandı (gömülü değil — sorgu katmanından filtrelenebilir olmalı); "ReminderAcknowledged" sıradan bir `FieldValueChanged` yazımın semantik adı. Gerçek Postgres+HTTP entegrasyon testiyle kanıtlı (geçmiş+onaylanmamış döner, onaylandıktan sonra dönmez, gelecekteki/hiç ayarlanmamış hiç dönmez).
- [x] F1-T7 Board görünümü, ek kod gerekmeden `status` alanına göre gruplayabildiği regresyon testiyle doğrulanır. `status-group-query.integration.test.ts`, gerçek Postgres+HTTP: `POST .../objects/query` ile `group: 'status'` (BoardView'ın gönderdiği aynı querySpec şekli) gerçek auto-seed'li `status` alanına karşı doğru gruplanıyor, ek implementasyon yok. Test-writer'ın ilk denemesinde gerçek bir yan etki bulundu: `status='done'` yazımı gerçek `TaskRecurrenceService`'i tetikliyor (PR3/PR4, zaten doğru davranış), her tamamlanan görev için yeni bir `todo` görevi üretiyor — testin `todo` grubu sayımı buna göre düzeltildi (kandırılmış/zayıflatılmış bir assertion değil, security-reviewer tarafından da doğrulandı).

## İlerleme Notu

- **PR1** (branch: `feature/f1-t10-pr1-status-priority-fields`, [#34](https://github.com/sirfurkansahin/luminaos/pull/34), main'e squash-merge edildi): `status`/`priority` select alanlarının otomatik idempotent seed'i + seçenek şemasına `isDone` bayrağı.
- **PR2** (branch: `feature/f1-t10-pr2-checklist-item`, [#35](https://github.com/sirfurkansahin/luminaos/pull/35), main'e squash-merge edildi): `packages/core-objects`'e gömülü `ChecklistItem` değer tipi + `addChecklistItem`/`toggleChecklistItem`/`removeChecklistItem`/`reorderChecklistItem` komutları + `replayObject` katlama desteği (200 öğe üst sınırı, silinmiş nesne guard'i, yinelenen `itemId` reddi, reorder permütasyon doğrulaması). security-reviewer 3 bulgu (yinelenen id, tip guard eksikliği, hata mesajında ham girdi) raporladı, hepsi aynı PR içinde kapatıldı. `apps/server/src/objects/objects.service.ts`'deki `toLuminaObject` read-projeksiyonu `checklist: []` placeholder'ıyla güncellendi (bu yol `objects_view`'dan okuyor, checklist henüz projeksiyona dahil değil — komut yürütme yolu zaten `replayObject` kullanıyor, dolayısıyla etkilenmiyor).
- **PR3** (branch: `feature/f1-t10-pr3-recurrence-generation`, [#38](https://github.com/sirfurkansahin/luminaos/pull/38), main'e squash-merge edildi): ADR-0010 uygulandı — `packages/core-objects`'te `RelationKind`'a `'recurrenceOf'` (döngü/tekillik doğrulaması yok, ADR-0010 §a) ve `Relation`'a opsiyonel `causationEventId`; `apps/server/src/recurrence/TaskRecurrenceService.generateNextOccurrence` `causationEventId`'den türetilen deterministik stream/event id'lerle (RFC4122 v5, `node:crypto` üzerinde, harici bağımlılık eklenmeden) yeni `task` nesnesi + `recurrenceOf` ilişkisini tek çağrıda, tekrarlı/eşzamanlı çağrılarda bile idempotent yazıyor (Layer B — `EventStoreService.append`'in kendi idempotent-replay'i üzerinden). security-reviewer 3 bulgu raporladı (workspaceId türetime dahil edilmemesi/cross-workspace collision riski, fieldKey için prototype-pollution guard eksikliği, girdi doğrulaması eksikliği), hepsi aynı PR içinde kapatıldı + regresyon testleri eklendi. Bu servis henüz İZOLE — hiçbir yerden çağrılmıyor; `task`'a `recurrenceRule` alanı da henüz eklenmedi (madde 4'ün ilk cümlesi).
- **PR4** (branch: `feature/f1-t10-pr4-isdone-transition-wiring`, [#40](https://github.com/sirfurkansahin/luminaos/pull/40), main'e squash-merge edildi): `recurrenceRule` alanı `packages/core-objects`'e eklendi (gömülü, opsiyonel `LuminaObject` alanı — `checklist`'in PR2'deki gerekçesiyle aynı: F1-T2'nin `FieldType` şeması yapılandırılmış/opsiyonel-üyeli bir değeri temsil edemiyor) + `setRecurrenceRule`/`clearRecurrenceRule` komutları + replay katlama. `apps/server`'da `detectStatusDoneTransition` saf fonksiyonu eklendi (yalnızca gerçek false→true geçişini tespit eder, ADR-0010 §f); `ObjectsService.setFieldValues` kendi `append`'i başarıyla tamamlandıktan SONRA `TaskRecurrenceService.generateNextOccurrence`'ı tetikleyici `FieldValueChanged` olayının kendi id'siyle çağırıyor. security-reviewer 1 gerçek bulgu raporladı (recurrence tetikleyicisinin hatası, zaten commit edilmiş `status` yazımını HTTP düzeyinde başarısız gösteriyordu) — `try/catch` ile izole edildi (log-and-continue, AI-refresh zamanlamasındaki fire-and-forget desenle aynı gerekçe) ve regresyon testiyle kanıtlandı.
- **Checklist property-based test** (branch: `test/f1-t10-checklist-property-based`, [#42](https://github.com/sirfurkansahin/luminaos/pull/42), main'e squash-merge edildi): madde 2'nin son açık gereksinimi kapatıldı — `packages/core-objects/src/checklist-replay-property.test.ts`, test-only, implementasyon değişmedi.
- **PR5** (branch: `feature/f1-t10-pr5-reminder-fields`, [#43](https://github.com/sirfurkansahin/luminaos/pull/43), main'e squash-merge edildi): `remindAt`/`remindAcknowledged` `workspaces.service.ts`'in `seedTaskFields`'ına eklendi (F1-T2 Custom Field, PR1'in status/priority deseniyle aynı — checklist/recurrenceRule'in aksine sorgu katmanından filtrelenebilir olmak zorunda olduğu için gömülü `LuminaObject` alanı DEĞİL). `remindAcknowledged`'a `defaultValue: false` zorunlu (yoksa checkbox `equals:false` sorgusu SQL `NULL`'a karşı hiçbir zaman eşleşmez). Yeni sorgu katmanı kodu/komut/olay tipi yok — mevcut mekanizmalar aynen kullanıldı. security-reviewer: 0 bulgu. Test-writer'ın kendiyle çelişen bir "sanity" testi (seed'in henüz var olmadığı red-state'i varsayan, ama seed koşulsuz hale gelince kalıcı olarak yanlış çıkan) implementer'ın tespiti üzerine kaldırıldı.
- **Board status gruplama regresyon testi** (branch: `test/f1-t10-board-status-grouping-regression`, main'e squash-merge edilecek): Kabul Kriterleri'nin son açık maddesi kapatıldı — `apps/server/src/objects/status-group-query.integration.test.ts`, test-only, implementasyon değişmedi.
- **PR6 — madde 6 (UI): Görev detay paneli** — spec'in son açık kapsam maddesi, 7 alt-PR'a bölündü (2 backend + 1 `packages/ui` bileşeni + 4 `apps/web`), plan: `precious-roaming-harbor` (tek onay tüm alt-PR'ları kapsadı). Keşif aşamasında bir arka plan boşluğu bulundu: `checklist`/`recurrenceRule` PR2/PR4'te komut katmanında vardı ama hiçbir HTTP route'a bağlı değildi ve `objects_view` projeksiyonu bu alanları hiç taşımıyordu (`toLuminaObject()` `checklist: []` placeholder döndürüyordu) — bu yüzden UI işine başlamadan önce iki backend alt-PR gerekti.
  - **PR6a** ([#45](https://github.com/sirfurkansahin/luminaos/pull/45), main'e squash-merge edildi): `objects_view`'a `checklist`/`recurrence_rule` jsonb sütunları + migration (down script dahil) + projeksiyonun `ChecklistItemAdded/Toggled/Removed/Reordered`/`RecurrenceRuleSet/Cleared` olaylarını oku-değiştir-yaz ile katlaması; `toLuminaObject()` artık gerçek değerleri okuyor. security-reviewer 2 bulgu (non-idempotent `ChecklistItemAdded` foldu, `toLuminaObject`'te kör cast'ler) raporladı, ikisi de aynı PR'da kapatıldı.
  - **PR6b** ([#47](https://github.com/sirfurkansahin/luminaos/pull/47), main'e squash-merge edildi): 6 yeni HTTP route (`checklist/items` POST/DELETE, `checklist/reorder` POST, `recurrence-rule` POST/DELETE) — mevcut `archive`/`restore` action-route deseniyle birebir, yeni izin kapısı yok (kasıtlı parite, `WorkspaceMembershipGuard` yeterli). `packages/core-objects/src/index.ts`'te PR4'ten kalma bir eksik export (`recurrence-rule-commands.js`) bu PR'da bulunup düzeltildi. security-reviewer 1 gerçek bulgu (checklist metin/dizi alanlarında üst sınır yokluğu → `.max()` eklendi) + 1 bilgilendirme (guest rolünün yazma erişimi — kasıtlı, değiştirilmedi) raporladı.
  - **PR6d** ([#48](https://github.com/sirfurkansahin/luminaos/pull/48), main'e squash-merge edildi, PR6a/b'den bağımsız paralel yürüdü): `packages/ui`'ye `DateTimePicker` (native `<input type="date"|"datetime-local">` sarmalayan, F0-T7'nin "ihtiyaç anında ekle" ertelemesinin karşılandığı an — bu PR'ın kendi içinde iki tüketicisi var).
  - **PR6c** ([#49](https://github.com/sirfurkansahin/luminaos/pull/49), main'e squash-merge edildi): veri katmanı + panel iskeleti — `useObjectQuery`/`useObjectIdParam` (`?objectId=` deep-link, `useViewParam` ile aynı `useSyncExternalStore` deseni), `TaskDetailPanel.tsx` (yalnızca loading/bulunamadı/başlık), List/Table/Board satırlarına tıklama kablolaması, `App.tsx`'e mount.
  - **PR6e** ([#50](https://github.com/sirfurkansahin/luminaos/pull/50), main'e squash-merge edildi): `useFieldDefinitionsQuery` + `StatusPrioritySelect` (durum/öncelik için tekrar kullanılabilir, mevcut `useSetFieldValuesMutation` değişmeden) — bu PR, sonraki tüm mutation hook'ları için "paylaşılan mutation + per-call `onSuccess` invalidation" desenini kurdu.
  - **PR6f** ([#51](https://github.com/sirfurkansahin/luminaos/pull/51), main'e squash-merge edildi): `ChecklistWidget` + `useChecklistMutations` (kendi HTTP route'ları üzerinde optimistic update). security-reviewer 2 bulgu raporladı (geçici id'li satırlarda satır-içi yarış durumu, tüm-nesne snapshot'ıyla rollback'in eşzamanlı başka bir mutation'ın sonucunu ezme riski) — ikisi de ayrı bir implementer turuyla kapatıldı; sonuç "cerrahi/surgical, her zaman güncel cache'e karşı, yalnızca kendi alanını geri alan rollback" deseni oldu, bu PR6g'de de aynen tekrarlandı.
  - **PR6g** ([#52](https://github.com/sirfurkansahin/luminaos/pull/52), main'e squash-merge edildi, epiğin son alt-PR'ı): `useRecurrenceRuleMutations` (PR6f'nin cerrahi rollback deseniyle) + `RecurrenceRulePicker`; `ReminderPicker` PR6e'nin paylaşılan-mutation desenini kullandı (yeni mutation hook'u gerekmedi, çünkü `remindAt`/`remindAcknowledged` PR5'ten beri sıradan Custom Field). security-reviewer: 0 bulgu. Bu PR ile spec madde 6 ve dolayısıyla F1-T10'un tamamı kapandı.
- **Kalan:** `relations_view`'a `causation_event_id` kolonu + partial unique index (ADR-0010 §c Layer A — bu akış Layer B'yi kullandığından madde 4'ü bloklamadı, ileride Otomasyon Motoru/F2-E5 için hazır zemin bırakıldı, F1-T10 kapsamının dışında).
